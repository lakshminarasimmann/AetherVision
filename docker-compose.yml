services:
  backend:
    build:
      context: backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - /app/uploads
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build:
      context: frontend
    ports:
      - "80:80"
    depends_on:
      - backend
